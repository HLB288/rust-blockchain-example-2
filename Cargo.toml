[package]
name = "blockchain-example"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.40"
sha2 = "0.10.8"
tokio = { version = "1", features = ["full"]}
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

